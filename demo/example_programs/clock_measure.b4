include sys.consts

var u16 value
var u16 freq
sys.print_string("kHz", 6, 0)

while true
  // reset the timer
  *SYS_TIMER_ADDR = 0

  for var u16 i; i < 20; i++
    // wait a few cycles

  value = *SYS_TIMER_ADDR
  freq = 7100 / value
  sys.print_u16(freq, 0, 0, true)
